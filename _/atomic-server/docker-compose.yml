version: "3.2"

volumes:
  storage:

services:
  atomic-server:
    container_name: atomic-server
    image: docker.getoutline.com/joepmeneer/atomic-server:v0.37.0
    ports:
      - "8020:3000"
    volumes:
      - storage:/atomic-storage
